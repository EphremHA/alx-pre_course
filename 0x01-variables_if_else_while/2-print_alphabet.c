#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 97;
	int z = 122;

	for (a >= 97; a <= 122; ++a)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
